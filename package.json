{
  "name": "openclaw-wordpress-rag-skill",
  "version": "0.1.0",
  "private": true,
  "description": "OpenClaw skill project for WordPress + WooCommerce RAG pipeline",
  "scripts": {
    "fetch:source": "node scripts/fetch-source-data.js",
    "validate:source": "node scripts/validate-source.js",
    "build:embeddings": "node scripts/generate-embeddings.js",
    "build:embeddings:force": "node scripts/generate-embeddings.js --force",
    "pipeline": "node scripts/embedding-worker.js",
    "pipeline:rebuild": "node scripts/embedding-worker.js --skip-fetch",
    "test:openai": "node scripts/test-openai-connectivity.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "openai": "^4.104.0",
    "openspec": "^0.0.0"
  }
}
